---
# - name: devices
#   debug:
#     var: ansible_facts.devices[item]
#   loop: "{{emptyDrives}}"

- name: Empty Drives
  debug:
    var: "ansible_facts.devices[item].links.ids[0]"
  loop: "{{emptyDrives}}"

- name: UniStor VGs
  debug:
    msg: "{{ unistorVg }}"
