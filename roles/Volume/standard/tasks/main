---
- name: Create volume per device
  lvol:
    vg: "{{item}}"
    thinpool: pool
    lv: "btrfs.{{n}}"
    size: 100%FREE
  loop: "{{unistorVg}}"
  loop_control:
    index_var: n

# - name: debug
#   debug:
#     msg: "vol on {{item}}"
#   loop: "{{unistorVg}}"
