---
- name: Get Partition information
  parted:
    device: "/dev/{{item.key}}"
    unit: MiB
  loop: "{{ drive | dict2items }}"
  loop_control:
    label: "{{ item.key }}"
  register: partition
  become: true


# - name: Part info
#   debug:
#     var: partition.results[0]
