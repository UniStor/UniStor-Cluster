---

- name: Create One Large Volume Group containing All Drives
  lvg:
    vg: "{{unistor.vgName}}"
    pvs: "{{unistor.pvs}}"
    pesize: "{{ unistor.pesize }}"
  become: true

# Future Options:
    # pv_options: '-Z y'
