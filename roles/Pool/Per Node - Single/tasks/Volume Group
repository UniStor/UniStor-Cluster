---

- name: Create One Large volume group with Every Drive
  lvg:
    vg: "{{partPrefix}}{{vgPrefix}}"
    pvs: "/dev/disk/by-id/{{ansible_facts.devices[item].links.ids[0]}}-part1"
    pesize: "{{ physicalExtentSize }}"
    pv_options: '-Z y'
  become: true
