---

- name: Debug
  debug:
    # var: ansible_facts.devices[item]
    msg:
      - "{{ ansible_facts.devices[item].links.ids[0] }}"
      - "{{ ansible_facts.devices[item].size }}"
  loop: "{{emptyDrive}}"
