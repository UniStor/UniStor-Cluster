---

- name: Create thin pool
  lvol:
    vg: "{{vgPrefix}}.{{ansible_facts.devices[item].links.ids[0]}}"
    thinpool: "{{poolPrefix}}.{{ansible_facts.devices[item].links.ids[0]}}"
    size: '100%FREE'
  loop: "{{emptyDrives}}"
